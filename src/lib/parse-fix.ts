/* eslint-disable @typescript-eslint/no-unused-vars */
import { ID, Query, Permission, Role, storage } from 'appwrite';
import { uploadToServer } from '@/lib/upload';

export async function dummy() {
  // placeholder to ensure file parses during temporary edits
  return true;
}
